services:
  api:
    image: ${API_IMAGE_NAME}:${API_IMAGE_TAG}
    pull_policy: never

  nginx:
    ports:
      - "${API_PORT}:80"
      - "${HTTPS_PORT}:443"
    volumes:
      - "/etc/letsencrypt:/etc/letsencrypt:ro"